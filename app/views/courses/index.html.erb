
<h3 id="login">Login</h3>
<% form_for @user, :url => session_url do |f| %>
    <%= f.label :username, 'Username' %>:
    <%= f.text_field :username, :size => 15 %><br />
    <%= f.label :password, 'Password' %>:
    <%= f.password_field :password, :size => 15 %><br />
		<%= link_to :Register, :new_user%> <%= submit_tag "OK"%>
<% end %>

<div id="courses">
	<ul id="courseListing">
	<% @courses.each do |course| -%>
		<li><%= link_to (h course.title), course %></li>
	<% end %>
	</ul>
	
</div>

<%= link_to 'New course', new_course_path %>

<%-# @courses.each do |course| %>
<%-#h course.description %>
<%-# link_to 'Edit', edit_course_path(course) %>
<%-# link_to 'Destroy', course, :confirm => 'Are you sure?', :method => :delete %>
<%-# end %>
